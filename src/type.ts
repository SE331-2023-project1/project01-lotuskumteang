export interface EventItem {
  studentId: number,
    name: string,
    surname: string,
    image: string,
    courseId: string[]
}

export interface StudentItem {
  studentId: number,
    name: string,
    surname: string,
    image: string,
    courseId: string[]
}

export interface Advisor {
  advisorId: string;
  name: string;
  surname: string;
  image: string;
}

